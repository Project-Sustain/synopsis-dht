apply plugin: 'java-library-distribution'

dependencies {
    compile project(':dht')
    compile project(':client')
    compile project(':samples')
}

distributions {
    main {
        baseName = 'synopsis2'
    }
}